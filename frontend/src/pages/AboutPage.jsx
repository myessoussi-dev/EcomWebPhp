export default function AboutPage() {
  return <section className="section container"><div className="page-hero__inner"><span className="eyebrow">About</span><h1 className="section-title">A refined storefront for practical products.</h1><p className="section-copy">EcomWeb is structured as a complete React frontend for the project backend. The experience uses reusable product, category, cart, account, and admin components with a warm editorial visual system.</p></div><div className="grid grid--3" style={{ marginTop: '2rem' }}>{['Curated catalog', 'Fast checkout', 'Scalable admin'].map((title) => <div className="panel" key={title}><span className="eyebrow">{title}</span><h2 style={{ marginTop: '1rem' }}>{title}</h2><p className="section-copy">Clean UI patterns ready to connect to production data and operations.</p></div>)}</div></section>
}
